[package]
name = "deterministic-card-game"
version = "0.2.0"
edition = "2024"
description = "Run Morgan's game from episode 120 of A Problem Squared."

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
rand = "0.10.2"
